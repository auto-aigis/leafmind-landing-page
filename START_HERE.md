# 🚀 LeafMind - Complete Deployment Walkthrough\n\n## Day-Of Deployment (1 hour total)\n\nThis is the ONLY file you need to follow to get from code to live with smoke test passing.\n\n### Phase 1: Supabase Setup (15 minutes)\n\n#### Step 1.1: Create Supabase Project\n```\n1. Go to https://supabase.com\n2. Click \"New Project\"\n3. Fill:\n   - Name: leafmind\n   - Password: [strong password]\n   - Region: [closest to your users]\n4. Click \"Create new project\"\n5. Wait 2-3 minutes for init\n```\n\n#### Step 1.2: Get Credentials\n```\n1. Supabase dashboard → Settings → API\n2. Copy:\n   NEXT_PUBLIC_SUPABASE_URL=[Project URL]\n   NEXT_PUBLIC_SUPABASE_ANON_KEY=[Anon (public) Key]\n3. Save these values (you'll need them twice)\n```\n\n#### Step 1.3: Create Waitlist Table\n```\n1. Supabase dashboard → SQL Editor → New Query\n2. Copy entire content of: frontend/supabase-migrations/001-waitlist-table.sql\n3. Paste into editor\n4. Click \"Run\"\n5. Wait for success message\n6. Left sidebar → Table Editor → Click \"waitlist\"\n7. Verify table shows 0 rows\n```\n\n### Phase 2: Render Deployment (20 minutes)\n\n#### Step 2.1: GitHub Push\n```bash\ncd frontend\ngit add -A\ngit commit -m \"LeafMind: Waitlist + Auth + Payments\"\ngit push\n```\n\n#### Step 2.2: Connect to Render\n```\n1. Go to https://render.com\n2. Click \"New +\" → \"Web Service\"\n3. Select your GitHub repo (auto-appears if connected)\n4. Fill form:\n   - Name: leafmind\n   - Environment: Node\n   - Build Command: npm ci && npm run build\n   - Start Command: npm run start\n   - Instance: Free (for now)\n5. Click \"Create Web Service\"\n6. Wait for build (will say \"Your service is live\" when done)\n```\n\n#### Step 2.3: Add Environment Variables\n```\n1. Render dashboard → Your service → Environment\n2. Add variables (copy from Step 1.2):\n   NEXT_PUBLIC_SUPABASE_URL=[your-url]\n   NEXT_PUBLIC_SUPABASE_ANON_KEY=[your-key]\n3. Click Save\n4. Render auto-redeploys (2-3 min)\n5. Wait for \"Your service is live\" message\n```\n\n#### Step 2.4: Get Your URL\n```\nRender dashboard → Copy URL shown at top\nExample: https://leafmind-xxxxx.onrender.com\n```\n\n### Phase 3: Smoke Test (5 minutes) ← THIS IS THE GO-NO-GO GATE\n\n#### Step 3.1: Access Landing Page\n```\nBrowser: https://leafmind-xxxxx.onrender.com?source=twitter\n\nVerify:\n✓ Page loads (no 502/404)\n✓ \"LeafMind\" header visible\n✓ \"Sign In\" and \"Sign Up\" links visible\n✓ Waitlist form visible with email input and button\n```\n\n#### Step 3.2: Submit Test Email\n```\n1. Type: test@example.com\n2. Click \"Join Waitlist\"\n3. Verify inline message appears:\n   Green box with checkmark: \"You're on the list! We'll be in touch soon 🌿\"\n```\n\n#### Step 3.3: Verify in Supabase (CRITICAL)\n```\n1. Supabase dashboard → Table Editor → waitlist\n2. Verify new row exists:\n   - email: test@example.com\n   - source: twitter ← THIS MUST BE 'twitter' NOT 'direct'\n   - submitted_at: [now]\n```\n\n**If row exists with source='twitter': ✅ DEPLOYMENT SUCCESSFUL**  \n**If no row or source='direct': ❌ TROUBLESHOOT (see below)**\n\n#### Step 3.4: Test Duplicate Handling\n```\n1. Submit same email again: test@example.com\n2. Verify blue message: \"You're already on the list!\"\n3. Verify NO new row added to Supabase\n```\n\n#### Step 3.5: Test Different Source\n```\n1. Visit: https://leafmind-xxxxx.onrender.com (no ?source=)\n2. Submit: test2@example.com\n3. Supabase → Verify source='direct' (default)\n```\n\n### Phase 4: Verification Complete\n\nYou now have:\n- ✅ Landing page with waitlist form\n- ✅ Supabase database storing signups\n- ✅ Source tracking (?source=twitter → stored correctly)\n- ✅ Duplicate prevention\n- ✅ Analytics ready\n\n**Campaign is ready to launch!**\n\n---\n\n## Troubleshooting (if smoke test fails)\n\n### Form submits but no row appears in Supabase\n\n**Check #1: Are credentials correct?**\n```\n1. Render dashboard → Environment\n2. Verify NEXT_PUBLIC_SUPABASE_URL and NEXT_PUBLIC_SUPABASE_ANON_KEY match\n3. Supabase Settings → API (double-check values)\n4. If wrong, update Render env vars and redeploy\n```\n\n**Check #2: Does waitlist table exist?**\n```\n1. Supabase → Table Editor\n2. Do you see \"waitlist\" in left sidebar?\n3. If not: SQL Editor → New Query → Paste 001-waitlist-table.sql → Run\n```\n\n**Check #3: Browser console errors?**\n```\n1. Press F12 (Developer Tools)\n2. Click Console tab\n3. Any red errors?\n4. If CORS error: Supabase RLS policy issue\n5. If other error: Supabase key issue\n```\n\n**Check #4: Enable RLS policies (if needed)**\n```\nSupabase → Authentication → Policies\nIf strict RLS enabled, create policy:\n- Policy name: Allow anonymous inserts\n- Table: waitlist\n- Type: INSERT\n- Clause: (true)\n- Save\n```\n\n### Row exists but source='direct' instead of 'twitter'\n\n**Issue**: Query parameter not being read\n\n**Fix**:\n1. Browser → Check URL bar: does it have `?source=twitter`?\n2. If not, click link again: `https://leafmind-xxxxx.onrender.com?source=twitter`\n3. Re-submit form\n\n### Landing page loads but form is broken\n\n**Check**: Network tab (F12 → Network → Filter \"waitlist\")\n1. Is insert request being sent to Supabase?\n2. What status code?\n   - 201: Success (but might not show message)\n   - 409: Unique constraint violation (duplicate)\n   - 401: Auth error (credentials wrong)\n   - 5xx: Supabase error\n\n### Page doesn't load at all (502 Bad Gateway)\n\n**Render build failed**\n```\n1. Render dashboard → Logs\n2. Scroll to top\n3. Look for build errors\n4. Common: Missing dependency\n5. Fix: Add to frontend/package.json, push, redeploy\n```\n\n---\n\n## Success! Next Steps\n\n### Before Twitter Campaign (by June 15)\n\n1. **[Optional] Configure Paddle** (if you want subscriptions)\n   - Get client token\n   - Create products and price IDs\n   - Add env vars to Render\n\n2. **[Optional] Set up backend** (if auth needed)\n   - Deploy FastAPI to Render\n   - Set NEXT_PUBLIC_API_URL\n\n3. **Monitor**:\n   - Visit landing page daily\n   - Verify no errors\n   - Check Supabase table growing\n\n### June 20 - Campaign Launch\n\n```\nTweet: \"Join LeafMind today →\" [link]\nhttps://leafmind-xxxxx.onrender.com?source=twitter\n```\n\nThen:\n1. Monitor Supabase: `SELECT COUNT(*) FROM waitlist WHERE source='twitter';`\n2. Check Vercel Analytics for page views\n3. Calculate conversion: (twitter_signups / twitter_views) × 100%\n\n---\n\n## One-Page Cheat Sheet\n\n| Step | Command/URL | Expected Result |\n|------|-------------|----------------|\n| 1. Create Supabase | https://supabase.com | Project created |\n| 2. Get credentials | Supabase → Settings → API | Copy URL + Key |\n| 3. Create table | SQL Editor + paste 001-waitlist-table.sql | Table in Table Editor |\n| 4. Push code | git push | Render detects push |\n| 5. Add env vars | Render → Environment | Save → Redeploy |\n| 6. Test landing | https://leafmind-xxxxx.onrender.com?source=twitter | Page loads |\n| 7. Submit email | test@example.com | Success message ✅ |\n| 8. Check Supabase | Table Editor → waitlist | Row with source='twitter' ✅ |\n\n**If all ✅: Ready for campaign!**\n\n---\n\n## Support\n\nFor detailed setup: See `frontend/SUPABASE_SETUP.md`  \nFor full deployment guide: See `frontend/DEPLOYMENT_GUIDE.md`  \nFor quick reference: See `frontend/QUICK_REFERENCE.md`  \nFor all docs: See `frontend/INDEX.md`\n\n---\n\n**Timeline**: 1 hour to live  \n**Go-No-Go**: Smoke test shows Supabase row with source='twitter'  \n**Campaign Date**: June 20, 2026  \n**Status**: Ready for deployment\n"